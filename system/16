# 'cron' is the name of the tool, 'crontab' is generally the file that lists the jobs that cron will be executing.
# Run crontab -e to add a cron job eg. add this to a crontab:
# 0,4,9,14,19,24,29,34,39,44,49 * * * * sudo service ssh restart
# to restart ssh every 5 min